const { predict, score } = require('./moderationController.js');

describe('Moderation Controller', () => {
  it('should predict content moderation', async () => {
    // Testez la fonction predict
  });

  it('should calculate content score', async () => {
    // Testez la fonction score
  });
});
